// CSVStat_Dataset — loads the dataset properties (label, source, updated, …)
// to a worksheet. Depends on: CSVStat (connection only), FilePath (parameter).
let
    Result = CSVStat
in
    Result[Dataset]
