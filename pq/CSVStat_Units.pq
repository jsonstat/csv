// CSVStat_Units — unit info for metric dimensions (decimals, label, symbol,
// position). Empty table when no metric units are declared. Depends on: CSVStat, FilePath.
let
    Result = CSVStat
in
    Result[Units]
